{
  "name": "exercise-logger",
  "private": true,
  "scripts": {
    "deploy": "npm --prefix server run build && npm --prefix client run build && pm2 restart nordCore --update-env",
    "logs": "tail -f server/logs/*.md"
  },
  "devDependencies": {
    "tsx": "^4.23.13"
  }
}
